[package]
name = "lemmalog"
version = "0.2.0"
edition = "2021"
description = "A Datalog engine for LLM context management: bi-temporal, provenance-annotated, incrementally maintained."
license = "MIT"

[dependencies]
serde_json = { version = "1", optional = true }
ureq = { version = "2", optional = true }

[[bin]]
name = "lemmajevgaun-mcp"
path = "src/bin/lemmalog-mcp.rs"
required-features = ["gate"]

[[bin]]
name = "lemmajevgaun-revise"
path = "src/bin/revise.rs"
required-features = ["mcp"]

[[bin]]
name = "lemmalog-bench"
required-features = ["llm"]

[[bin]]
name = "lemmalog-cli"
required-features = ["mcp"]

[[test]]
name = "jev_test"
required-features = ["jev"]

[[example]]
name = "jev_align"
required-features = ["jev"]

[[example]]
name = "lmstudio"
required-features = ["llm"]

[[example]]
name = "llm_rules"
required-features = ["llm"]

[[example]]
name = "longmemeval"
required-features = ["llm"]

[features]
# Real-model integration: OpenAI-compatible chat + embeddings (LM Studio,
# llama.cpp server, OpenAI, etc.). Enable with --features llm.
llm = ["dep:ureq", "dep:serde_json"]
# MCP server binary (Model Context Protocol over stdio) for agent CLIs
# (Claude Code, Kimi CLI, ...). Needs only serde_json.
mcp = ["dep:serde_json"]
# The deployable product: one MCP surface for facts, evidence gates, and JEV.
gate = ["mcp", "jev"]
# TypeSafe System One (`jev`): typed questions, calibrated answers. Supplies
# the confidence the semiring wants and the entity-alignment judgment that
# `canonical::reconcile` currently asks a text model to write in prose.
jev = ["dep:ureq", "dep:serde_json"]
